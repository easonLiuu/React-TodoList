import React, { FC } from "react";

const Count: FC = () => {
  return (
    <div>
      <span>count</span>
      <button>+</button>
      <button>-</button>
    </div>
  );
};

export default Count;
